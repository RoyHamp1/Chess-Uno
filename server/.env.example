# Postgres on this machine → use localhost (not the pgAdmin "server name").
# Remote DB → use the real host or IP from pgAdmin → Properties → Connection → Host.
DATABASE_URL=postgresql://YOUR_USER:YOUR_PASSWORD@localhost:5432/ChUno
JWT_SECRET=replace-with-a-long-random-string-in-production
